# DK Music Environment Configuration
# API Keys are securely processed on the backend server (server.ps1)
# Keep your API keys confidential and NEVER commit real keys to source control.

GEMINI_API_KEY=your_gemini_api_key_here
PORT=5500
